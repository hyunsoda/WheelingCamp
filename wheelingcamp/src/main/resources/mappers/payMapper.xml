<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.wheelingcamp.pay.model.PaymentMapper">
	
<!--  대여하기 pay 테이블에 삽입 -->	
<insert id="putPay">	
INSERT INTO "PAY" 
VALUES(SEQ_PAY_NO.NEXTVAL, #{totalAmount}, #{orderName}, #{paymentId})
</insert>
<!--  대여하기 pay 테이블에 삽입 -->	
	
	
<!--  구매하기 pay 테이블에 삽입 -->		
<insert id="putRent">
INSERT INTO "RENT"
VALUES(SEQ_RENT_NO.NEXTVAL, #{rentDate}, 
#{expectDate}, DEFAULT, NULL, ${memberNo}, SEQ_PAY_NO.CURRVAL)	
</insert>
<!--  구매하기 pay 테이블에 삽입 -->

	<!-- 결제 횟수 카운트-->
    <select id="getPayCount">
        SELECT COUNT(*)
        FROM "PAY"
        WHERE MEMBER_NO = #{memberNo}
    </select>

    <!-- 첫 결제 시 10번 뱃지 수여 -->
    <update id="updateFirstPayBadge">
        UPDATE "MEMBER_BADGE"
        SET BADGE_FL = 'Y'
        WHERE MEMBER_NO = #{memberNo} 
        AND BADGE_NO = 10
    </update>

</mapper>
